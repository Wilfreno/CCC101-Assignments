#include <stdio.h>
#include <stdlib.h>

int main (int argc, char *argv[]) {

    int n,
        k, 
        between;
    
    
    if (n < -k || n > +k) {

        between = 0;

    }
    else {

        between =1;
    }


    return EXIT_SUCCESS;
}