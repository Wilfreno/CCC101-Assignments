#include <stdio.h>
#include <stdlib.h>

int main (int argc, char *argv[]) {
    
    int item, 
        product;
    if (item != 0)
    {
        product = product * item;
        printf("%d", product);
    }
    else {
        printf("%d", product);
    }
    
 
    return EXIT_SUCCESS;
}