#include <stdio.h>

#define PI 3.14159
#define MAX_I 1000

int main () {

    double  x, 
            y = -1.0;

    int a = 3, 
        b = 4,
        i;



    // i = a % b;
    // printf("A.\nThe answer is %d \n", i);

    // i = (989 - MAX_I) / a;
    // printf("B.\nThe answer is %d \n", i);

    // i = b % a;
    // printf("C.\nThe answer is %d \n", i);

    // x = PI * y;
    // printf("D.\nThe answer is %f \n", x);

    // i = a / -b;
    // printf("E.\nThe answer is %d \n", i);

    // x = a / b;
    // printf("F.\nThe answer is %f \n", x);

    // // x = a % (a / b);
    // // printf("G.\nThe answer is %f \n", x);

    // // i = b / 0;
    // // printf("H.\nThe answer is %d \n", i);

    // i = a % (990 - MAX_I);
    // printf("I.\nThe answer is %d \n", i);

    // i = (MAX_I - 990) / a;
    // printf("J.\nThe answer is %d \n", i);

    // x = a / y;
    // printf("K.\nThe answer is %f \n", x);

    // i = PI * a;
    // printf("L.\nThe answer is %d \n", i);

    // x = PI / y;
    // printf("M.\nThe answer is %f \n", x);

    // x = b / a;
    // printf("N.\nThe answer is %f \n", x);
    
    // i = (MAX_I - 990) % a;
    // printf("O.\nThe answer is %d \n", i);

    // // i = a % 0;
    // // printf("P.\nThe answer is %d\n", i);

    // i = a % (MAX_I - 990);
    // printf("Q.\nThe answer is %d \n", i);


    return 0;
}