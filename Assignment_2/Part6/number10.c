#include <stdio.h>
#include <stdlib.h>

int main (int argc, char *argv[]) {
 
    int zero_count,
        minus_sum,
        plus_sum,
        x;

    if(x == 0){

        zero_count++;
    }
    else if(x < 0) {

        minus_sum = minus_sum + x;
    }
    else if (x > 0) {

        plus_sum = plus_sum + x
    }

    return EXIT_SUCCESS;
}